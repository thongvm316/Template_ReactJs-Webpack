// import { render } from "react-dom";

// import App from "./components/App";

// render(<App />, document.getElementById("root"));

const forTest = {
  a: 'fd',
  b: 'fdfdf',
  c: 2323,
}

console.log(forTest)
